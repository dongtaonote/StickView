<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stickview">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".coordinatorLayout.activity.CoordinatorMainActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".scrollablelayout.activity.ScrollAbleMainActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".texttopview.activity.TextTopMainActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".sticknavi.activity.StickNaviActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>